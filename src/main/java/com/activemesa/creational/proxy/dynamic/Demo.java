package com.activemesa.creational.proxy.dynamic;

/**
 * Dynamic proxy is a proxy which is constructed at runtime as opposed to compile time.
 * At runtime you get an instance of an object and we create a wrapper around it and intercept any single call to it
 * and its methods
 */
public class Demo {
}
